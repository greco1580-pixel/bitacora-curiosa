/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Reemplazar por los dominios reales donde se alojen las fotos
    // (por ejemplo un CDN o el propio /public) cuando haya imágenes definitivas.
    remotePatterns: []
  }
};

module.exports = nextConfig;
